using UnityEngine;

namespace ARMagicBar.Resources.Scripts.GizmoUI
{
    public class ShowInteractionsOrTransformUI : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
